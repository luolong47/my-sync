# 产品规则草案

## 1. 目标定义

My Sync 的核心目标是：

- 让同一用户在不同设备上的配置文件保持一致
- 允许不同设备使用不同的本地文件路径
- 让用户同步的是“配置内容”，而不是“本地绝对路径”

本产品不是整机备份工具，也不是目录镜像工具，而是“少量关键配置文件的跨设备同步工具”。

## 2. 核心原则

### 2.1 同步内容，不同步本地路径

同一个同步项在不同设备上的本地路径可以不同。

例如：

- 设备 1：`C:\Users\LuoLong\.gitconfig`
- 设备 2：`C:\Users\admin\.gitconfig`

这两个路径虽然不同，但都可以绑定到同一个同步项 `git/.gitconfig`。

系统真正共享的是远端逻辑路径对应的文件内容，而不是本地路径字符串。

### 2.2 设备身份不参与内容分区

设备标识用于区分“是谁在同步、谁产生了变更、谁正在在线”，但不能参与远端主文件路径拼接。

错误模型：

- `remote_dir/client_id/git/.gitconfig`

正确模型：

- `remote_dir/<space_id>/git/.gitconfig`

设备信息应单独存放在元数据区域，例如：

- `remote_dir/<space_id>/.devices/<device_id>/status.json`

### 2.3 同步项是共享对象，路径绑定是设备私有对象

每个同步项应分为两层：

1. 共享层
2. 设备层

共享层定义“这是什么内容”，设备层定义“这份内容在本机落到哪里”。

## 3. 概念模型

### 3.1 同步空间

同步空间用于表示一组共享配置。

建议规则：

- 一个账号可拥有多个同步空间
- 默认同步空间为 `default`
- 同一空间内的所有设备共享同一份远端配置内容

示例：

- `my-sync/default/git/.gitconfig`
- `my-sync/default/pnpm/.npmrc`

### 3.1.1 产品配置同步规则

产品本身的配置也必须纳入同步空间，并存储到 WebDAV。

这里的“产品配置”不是指当前设备的全部运行状态，而是指跨设备共享后仍然成立的业务配置。

应纳入同步的内容：

- 同步空间内的同步项定义
- 每个同步项的名称
- 每个同步项的远端逻辑路径
- 每个同步项的启用状态
- 与同步项相关的共享规则

不应直接作为共享配置同步的内容：

- 当前设备的本地绝对路径
- 当前设备的临时运行状态
- 当前设备的窗口状态、主题、托盘状态
- 当前设备专属错误信息

建议将共享配置存放在类似以下位置：

- `remote_dir/<space_id>/.config/sync-items.json`

设备私有配置仍保留在本地，或存放到设备专属元数据区域，例如：

- `remote_dir/<space_id>/.devices/<device_id>/bindings.json`

### 3.2 设备标识

设备标识用于表示当前客户端实例。

用途：

- 记录最后写入者
- 记录在线状态
- 记录设备名、系统信息、最后同步时间
- 冲突日志溯源

限制：

- 不参与远端主文件路径
- 不决定配置是否共享

### 3.2.1 设备 ID 生成规则

为尽量满足“重装系统后仍可恢复为同一设备身份”的目标，设备 ID 建议采用“多硬件特征归一化后哈希”的方案。

建议参与计算的候选硬件特征包括：

- 机器 UUID
- 主板序列号
- BIOS 序列号
- CPU 标识的一部分
- 主系统盘序列号

生成规则：

1. 尽量采集多项硬件标识
2. 过滤空值、默认值、明显无效值
3. 对剩余字段做统一归一化处理
4. 按固定顺序拼接
5. 对拼接结果做哈希，得到 `device_id`

该方案的目标是：

- 在重装系统后，仍有较高概率得到相同的设备 ID
- 比“设备名 + MAC 地址哈希”更稳定
- 尽量减少因用户名、主机名、网卡变化带来的设备漂移

### 3.2.2 硬件特征过滤规则

采集到的候选值并不都可靠，必须过滤无效内容。

应过滤的内容包括：

- 空字符串
- 全 0 值
- `unknown`
- `to be filled by o.e.m.`
- `system serial number`
- 默认占位字符串
- 明显重复或无辨识度的通用值

过滤目标是避免把无意义值混入最终哈希，导致不同设备计算出相同结果，或同一设备在不同环境下漂移。

### 3.2.3 归一化规则

为减少同一硬件在不同系统接口下格式不同导致的问题，参与哈希前必须统一归一化。

建议规则：

- 去除首尾空白
- 统一为大写或统一为小写
- 去除中划线、空格等格式噪音
- 使用固定字段名进行拼接
- 字段缺失时直接跳过，不用占位假值补齐

拼接示例：

- `machine_uuid=<值>`
- `baseboard_serial=<值>`
- `bios_serial=<值>`
- `cpu_id=<值>`
- `system_disk_serial=<值>`

最终按固定顺序拼接后再做哈希。

### 3.2.4 稳定性与限制

该方案比“设备名 + MAC 地址哈希”更稳定，但仍不是绝对不变。

仍可能变化的情况包括：

- 更换主板
- 更换系统盘
- 虚拟机模板变化
- 厂商未提供可靠硬件序列号
- 某些系统接口权限受限导致可采集字段减少

因此产品层应表达为：

- 这是“高稳定设备 ID”方案
- 不是“绝对永不变化”的设备身份方案

### 3.2.5 与设备恢复的关系

设备 ID 使用该规则后，可用于重装系统后的设备身份恢复。

建议恢复逻辑：

1. 新装系统后重新计算当前设备的 `device_id`
2. 到 WebDAV 的设备元数据中查找是否存在相同 `device_id`
3. 若存在，则视为历史同一设备并恢复设备绑定关系
4. 若不存在，则作为新设备加入同步空间

这可以提升以下场景的体验：

- 重装系统后重新接入
- 清空本地程序数据后重新登录
- 更换用户名但未更换机器

### 3.3 同步项

同步项是跨设备共享的最小业务单位。

建议字段：

- `id`：稳定唯一标识
- `name`：显示名称
- `remote_path`：远端逻辑路径
- `enabled`：是否启用

示例：

- `id`: `git-config`
- `name`: `Git 配置`
- `remote_path`: `git/.gitconfig`

### 3.4 设备绑定

设备绑定表示“同一个同步项在当前设备上的本地落点”。

建议字段：

- `device_id`
- `sync_item_id`
- `local_path`
- `path_template`
- `exists_on_device`
- `last_bound_at`

说明：

- `local_path` 是当前设备私有配置
- 不同设备可为同一 `sync_item_id` 绑定不同路径
- 如果本地路径不存在，不应直接视为全局配置错误，而应进入“待绑定”状态

### 3.5 共享配置与设备配置的边界

为满足“设备 1 配好后，设备 2 只填 WebDAV 就能接着用”的目标，必须明确配置边界。

共享配置负责回答：

- 有哪些同步项
- 每个同步项叫什么
- 每个同步项同步到远端哪个逻辑路径
- 每个同步项是否启用

设备配置负责回答：

- 这个同步项在当前设备落到哪个本地路径
- 当前设备是否已经完成绑定
- 当前设备最近一次同步状态如何

例如：

共享配置定义：

- `Git 配置` -> `git/.gitconfig`

设备 1 本地绑定：

- `git-config` -> `C:\Users\LuoLong\.gitconfig`

设备 2 本地绑定：

- `git-config` -> `C:\Users\admin\.gitconfig`

这样设备 2 在只完成 WebDAV 配置后，就可以自动获取“存在一个名为 Git 配置 的同步项，并且它对应远端 `git/.gitconfig`”，但仍需要在本机解析或确认自己的本地绑定路径。

## 4. 路径规则

### 4.1 远端路径规则

远端主路径结构建议为：

`remote_dir/<space_id>/<remote_path>`

例如：

- `my-sync/default/git/.gitconfig`
- `my-sync/default/shell/powershell/Microsoft.PowerShell_profile.ps1`

禁止规则：

- 不允许自动拼接 `client_id`
- 不允许把设备名作为共享路径的一部分

### 4.2 本地路径规则

本地路径只在当前设备有效。

允许情况：

- 不同用户名导致的路径差异
- 不同盘符导致的路径差异
- 不同操作系统导致的路径差异

例如同一个同步项可绑定为：

- Windows：`C:\Users\LuoLong\.gitconfig`
- Windows：`C:\Users\admin\.gitconfig`
- macOS：`/Users/luolong/.gitconfig`

### 4.3 路径模板规则

为降低跨设备配置成本，建议支持路径模板。

优先支持：

- Windows：`%USERPROFILE%\.gitconfig`
- Windows：`%APPDATA%\Code\User\settings.json`
- macOS / Linux：`$HOME/.gitconfig`

建议规则：

- 模板仅用于本地解析
- 模板展开结果不上传到远端共享配置
- 展开失败时进入“待绑定”状态

### 4.4 跨设备自动路径推断规则

当设备 2 从 WebDAV 读取到设备 1 创建的共享同步项后，系统应尽可能自动推断当前设备的本地路径。

例如设备 1 已存在绑定：

- `C:\Users\LuoLong\.gitconfig`

当设备 2 登录同一同步空间后，系统不应机械复用这个绝对路径，而应推断出当前设备更合理的候选路径，例如：

- `C:\Users\admin\.gitconfig`

建议推断顺序：

1. 优先使用同步项的路径模板，例如 `%USERPROFILE%\.gitconfig`
2. 若无模板，则根据文件类型命中预置规则
3. 若仍无法确定，则基于当前系统用户目录生成候选路径
4. 若候选路径不存在或风险较高，则进入“待绑定”并要求用户确认

约束规则：

- 不允许将其他设备的绝对路径直接写入当前设备
- 不允许把 `C:\Users\LuoLong\...` 原样当作设备 2 的有效路径
- 自动推断失败时应安全降级为人工确认

## 5. 同步行为规则

### 5.0 启动引导与配置获取规则

新设备第一次接入时，最小操作目标应为：

1. 用户只输入 WebDAV 连接信息
2. 系统自动读取同步空间中的共享配置
3. 系统自动生成当前设备的本地绑定候选
4. 已成功推断的同步项直接进入同步
5. 未成功推断的同步项进入“待绑定”

这意味着设备 2 不需要重新手工创建设备 1 已经建立好的映射定义，只需要完成 WebDAV 配置并确认少量无法自动推断的本地路径即可。

### 5.1 上传流程

当某设备本地文件发生变化时：

1. 检测到本地文件变更
2. 读取文件内容
3. 与最近同步快照比较
4. 若内容已变化，则上传到共享远端路径
5. 更新同步项状态、设备状态和日志

示例：

1. 设备 1 修改 `C:\Users\LuoLong\.gitconfig`
2. 上传到 `my-sync/default/git/.gitconfig`

### 5.2 拉取流程

当某设备发现共享远端文件变化时：

1. 检查远端文件时间戳或内容哈希
2. 若远端内容有更新，则定位当前设备对该同步项的本地绑定
3. 将远端内容写入当前设备本地路径
4. 更新同步状态和日志

示例：

1. 设备 1 上传了新的 `git/.gitconfig`
2. 设备 2 下一轮同步检测到变化
3. 写入 `C:\Users\admin\.gitconfig`

### 5.2.1 前提条件规则

设备 2 要能够自动写入 `C:\Users\admin\.gitconfig`，前提不是“复制设备 1 的本地路径”，而是满足以下条件之一：

- 设备 2 已经拥有该同步项的本地绑定
- 设备 2 可依据路径模板自动解析出本地绑定
- 设备 2 可依据系统规则自动推断出安全候选路径

如果以上条件都不满足，则不能盲目写入未知路径，而应进入“待绑定”。

### 5.3 自动同步规则

自动同步应分为两部分：

- 本地变更触发上传
- 后台轮询触发拉取

建议：

- 启用文件系统监听时，优先使用监听触发上传
- 后台轮询始终保留，用于发现其他设备的远端变更
- 启动程序后自动执行一次同步

### 5.4 同步方向判定

每个同步项都应基于“本地内容、远端内容、最近同步快照”三者判断。

结果可分为：

- 本地领先，执行上传
- 远端领先，执行拉取
- 双方一致，不处理
- 双方均变化，进入冲突

## 6. 冲突规则

### 6.1 冲突定义

如果在最近一次共同基线之后：

- 本地内容发生变化
- 远端内容也发生变化
- 且两者不同

则判定为冲突。

### 6.2 冲突处理方式

应支持以下策略：

- 手动处理
- 以本地为准
- 以远端为准

默认建议：

- 默认使用手动处理

### 6.3 冲突展示要求

冲突信息应尽量可解释。

建议展示：

- 同步项名称
- 当前设备本地路径
- 远端逻辑路径
- 最近修改时间
- 最近写入设备标识
- 冲突来源说明

### 6.4 冲突副本规则

当选择保留本地版本时，可将远端版本写出为冲突副本。

命名建议：

- `<文件名>.remote-conflict-时间戳`

该规则仅作用于本地，不能写回共享主路径。

## 7. 新设备接入规则

### 7.1 新设备首次加入

新设备加入同一同步空间时，应能读取到共享同步项列表，但不强制立即拥有所有本地绑定。

首次启动建议流程：

1. 登录 WebDAV 信息
2. 选择或确认同步空间
3. 自动下载共享配置
4. 读取远端同步项清单
5. 对每个同步项尝试自动匹配本地路径模板
6. 自动匹配失败的同步项进入“待绑定”
7. 用户确认本地路径后纳入自动同步

### 7.1.1 自动获取映射定义规则

设备 2 在只配置 WebDAV 后，应能够自动获得设备 1 已创建的同步项定义。

应自动获取的内容：

- 同步项名称
- 同步项稳定 ID
- 远端逻辑路径
- 启用状态
- 路径模板或路径推断提示信息

不应自动照搬的内容：

- 设备 1 的本地绝对路径

例如：

设备 1 已配置：

- `Git 配置`
- 远端路径：`git/.gitconfig`
- 本地路径：`C:\Users\LuoLong\.gitconfig`

设备 2 接入后应表现为：

- 自动拿到 `Git 配置`
- 自动知道共享远端是 `git/.gitconfig`
- 自动尝试解析到 `C:\Users\admin\.gitconfig`
- 解析成功则直接同步
- 解析失败则进入“待绑定”，等待用户确认

### 7.2 待绑定状态

如果当前设备尚未为某同步项配置本地路径，则应显示“待绑定”，而不是“同步失败”。

待绑定状态下允许：

- 浏览远端内容
- 手动下载到本地
- 选择本地路径完成绑定
- 暂时忽略该同步项

## 8. 删除与解绑规则

### 8.1 删除同步项

删除同步项表示删除共享对象。

建议行为：

- 删除远端共享文件
- 删除该同步项定义
- 删除所有设备上的绑定关系

该操作应明确提示风险。

### 8.2 解绑本机路径

解绑仅影响当前设备，不影响共享同步项本身。

建议行为：

- 删除当前设备对该同步项的 `local_path` 绑定
- 保留远端共享文件
- 其他设备不受影响

## 9. 状态与日志规则

### 9.1 同步项状态

建议支持以下状态：

- `idle`：尚未同步
- `synced`：已同步
- `uploading`：上传中
- `downloading`：拉取中
- `conflict`：存在冲突
- `pending_bind`：待绑定
- `error`：异常

### 9.2 设备状态

每台设备可记录：

- 设备名称
- 设备标识
- 系统类型
- 最近同步时间
- 最近在线时间
- 最近错误信息

### 9.3 日志要求

日志建议包含：

- 时间
- 同步项名称
- 本地路径
- 远端逻辑路径
- 动作类型
- 结果
- 来源设备

动作类型示例：

- 本地变更上传
- 远端更新拉取
- 冲突检测
- 人工处理冲突

## 10. 迁移规则

### 10.1 旧结构识别

如果系统发现历史远端结构为：

`remote_dir/<client_id>/<remote_path>`

则应识别为旧版“按设备隔离”结构。

### 10.2 迁移目标

迁移后统一改为：

`remote_dir/<space_id>/<remote_path>`

设备信息迁移到：

`remote_dir/<space_id>/.devices/<device_id>/...`

### 10.3 迁移流程建议

建议首次升级时执行：

1. 检测旧版隔离目录
2. 提示用户当前结构会阻断跨设备共享
3. 提供“迁移到共享空间”入口
4. 将当前设备目录中的共享文件迁移到目标空间
5. 保留必要备份或迁移日志
6. 迁移完成后停止在主路径中使用 `client_id`

### 10.4 迁移风险提示

迁移时应提示：

- 如果多台设备都在旧结构下各自有不同版本，迁移前需要做冲突确认
- 迁移可能覆盖已有共享空间中的同名文件
- 迁移应尽可能支持预览和回滚

## 11. 用户心智定义

产品对用户的表达应尽量统一为：

- “同步项”表示一份跨设备共享的配置内容
- “本地路径”表示该同步项在当前设备上的落点
- “设备标识”表示当前设备身份
- “同步空间”表示一组共享配置

用户应理解为：

“我在不同设备上把同一个同步项绑定到各自的本地路径，任意设备修改后，其他设备会把共享内容同步到自己的本地文件。”

而不是：

“每台设备各自维护自己的远端副本。”

## 12. 最终结论

为满足核心需求，产品必须满足以下三条：

1. 远端内容路径必须是共享路径，不能包含 `client_id`
2. 本地绝对路径必须是设备私有绑定，而不是共享数据
3. 自动同步必须同时支持“本地上传”和“远端拉取”

同时还必须补充第四条：

4. 产品的共享配置定义必须同步到 WebDAV，这样新设备只配置 WebDAV 后就能自动获得已有同步项

只有这样，下面这个场景才会成立：

- 设备 1 修改 `C:\Users\LuoLong\.gitconfig`
- 内容上传到共享远端 `git/.gitconfig`
- 设备 2 检测到远端变化
- 内容写入 `C:\Users\admin\.gitconfig`

对应的新设备接入体验应为：

- 设备 1 已创建 `Git 配置` 同步项
- 共享配置已上传到 WebDAV
- 设备 2 只填写 WebDAV 信息即可拉取该同步项定义
- 系统自动将设备 1 的 `C:\Users\LuoLong\.gitconfig` 识别为“另一台设备的本地绑定”
- 系统在设备 2 上解析为 `C:\Users\admin\.gitconfig` 或给出候选路径
- 设备 2 完成本地绑定后即可与设备 1 共享同一份 `git/.gitconfig` 内容

这才符合 My Sync 作为“多设备配置同步工具”的产品初心。
