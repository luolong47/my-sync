# 具体改造细则

本文基于 [产品规则草案.md](/D:/Prjs/my-sync/产品规则草案.md) 与当前代码实现整理，目标是给出一份可直接指导后续开发的改造清单。

当前代码的核心现实是：

- 本地状态统一保存在 `state.json`
- 同步主流程围绕 `AppConfig.mappings` 展开
- WebDAV 远端主路径仍然拼接了 `client_id`
- 前端配置页直接编辑本地 `mappings`
- 新设备无法从 WebDAV 自动获取设备 1 已创建的同步项定义

因此本次改造的关键不是“补一个配置同步接口”，而是要把“共享配置”和“设备本地绑定”从现有单层模型中拆开。

## 1. 当前实现评估

### 1.1 现有数据模型

当前后端模型位于 [src-tauri/src/app/models.rs](/D:/Prjs/my-sync/src-tauri/src/app/models.rs)。

当前关键结构：

- `WebDavSettings`
  - 含 `base_url`、`username`、`password`、`remote_dir`、`client_id`
- `FileMapping`
  - 含 `id`、`name`、`local_path`、`remote_path`
- `AppConfig`
  - 含 `webdav`、`sync`、`mappings`
- `PersistedStore`
  - 含 `config`、`file_states`、`sync_logs`

问题：

- `FileMapping` 把“共享定义”和“设备本地绑定”混在一起
- `local_path` 被放进 `config.mappings`，导致它只能存在本地，不适合跨设备共享
- `client_id` 被放进 WebDAV 主路径，直接阻断跨设备共享内容

### 1.2 现有存储边界

当前本地持久化在 [src-tauri/src/app/storage.rs](/D:/Prjs/my-sync/src-tauri/src/app/storage.rs)。

现状：

- 所有配置和运行状态都序列化到本地 `state.json`
- `normalize_config` 会在本地为 `client_id` 自动生成时间戳型值
- `can_prepare_remote_root` 要求 `client_id` 非空

问题：

- 本地持久化边界过大，缺少“共享配置”和“设备私有配置”的拆分
- `client_id` 当前既像设备身份，又参与远端路径构造

### 1.3 现有远端路径规则

当前 WebDAV 路径逻辑位于 [src-tauri/src/app/webdav.rs](/D:/Prjs/my-sync/src-tauri/src/app/webdav.rs) 和 [src-tauri/src/app/storage.rs](/D:/Prjs/my-sync/src-tauri/src/app/storage.rs)。

现状：

- `join_remote_segments_with_client(base, client_id, extra)`
- `remote_root_segments()` 会把 `client_id` 作为路径段拼入
- `file_url()`、`list_remote_entries()`、`ensure_root_collection()` 都建立在这个规则之上

问题：

- 远端目录语义是 `remote_dir/client_id/...`
- 设备 1 和设备 2 永远不会访问同一份共享内容

### 1.4 现有同步流程

当前同步主流程位于 [src-tauri/src/app/sync.rs](/D:/Prjs/my-sync/src-tauri/src/app/sync.rs)。

现状：

- `sync_all_mappings()` 遍历 `config.mappings`
- `sync_single_mapping()` 直接读取 `mapping.local_path`
- 运行时状态 `file_states` 以 `mapping.id` 为 key
- 三方比较逻辑基于：
  - 本地文件内容
  - 远端文件内容
  - `last_synced_hash`

问题：

- 该流程默认每个 mapping 都同时包含共享定义和本地路径
- 无法表达“设备 2 已拿到共享同步项，但本机尚未绑定路径”
- 无法在没有 `local_path` 的情况下让某个共享同步项进入“待绑定”

### 1.5 现有前端配置结构

当前前端类型与状态位于 [src/types/app.ts](/D:/Prjs/my-sync/src/types/app.ts)、[src/utils/appState.ts](/D:/Prjs/my-sync/src/utils/appState.ts)、[src/stores/syncApp.ts](/D:/Prjs/my-sync/src/stores/syncApp.ts)、[src/components/views/SettingsView.vue](/D:/Prjs/my-sync/src/components/views/SettingsView.vue)。

现状：

- `AppConfig.mappings` 直接由前端编辑
- `remoteRootPath` 展示 `remoteDir/clientId`
- `canBrowseRemote` 依赖 `clientId`
- 设置页中 `clientId` 作为只读字段展示

问题：

- 前端 UI 心智仍然是“当前客户端自己的远端空间”
- 没有“同步空间”
- 没有“共享同步项”和“当前设备绑定”的区分
- 没有“待绑定”状态

## 2. 总体改造目标

本次改造必须同时实现以下目标：

1. 将远端内容路径从 `remote_dir/client_id/...` 改为 `remote_dir/<space_id>/...`
2. 将同步项拆为“共享配置定义”和“设备本地绑定”
3. 将共享配置定义同步到 WebDAV
4. 新设备只完成 WebDAV 配置后，即可自动拉取已有同步项定义
5. 新设备不得直接复用旧设备绝对路径，而应自动推断或进入“待绑定”
6. 设备 ID 规则升级为“多硬件特征归一化后哈希”

## 3. 目标数据模型

### 3.1 WebDAV 设置模型调整

改造对象：

- [src-tauri/src/app/models.rs](/D:/Prjs/my-sync/src-tauri/src/app/models.rs)
- [src/types/app.ts](/D:/Prjs/my-sync/src/types/app.ts)

建议替换 `WebDavSettings` 为以下语义：

- `base_url`
- `username`
- `password`
- `remote_dir`
- `space_id`
- `device_id`
- `device_name`
- `sync_interval_secs`
- `auto_sync`

处理规则：

- `space_id` 默认为 `default`
- `device_id` 使用硬件特征归一化后哈希生成
- `device_name` 用于展示，可允许后续手动修改
- `client_id` 字段进入兼容期，最终废弃

### 3.2 共享同步项模型

新增共享模型：

- `SyncItem`
  - `id`
  - `name`
  - `remote_path`
  - `path_template`
  - `enabled`

说明：

- `SyncItem` 进入共享配置文件，存放到 WebDAV
- 这是跨设备共享的唯一同步定义来源

### 3.3 设备绑定模型

新增设备模型：

- `DeviceBinding`
  - `device_id`
  - `sync_item_id`
  - `local_path`
  - `binding_status`
  - `last_bound_at`

说明：

- `binding_status` 至少支持 `bound`、`pending_bind`
- `DeviceBinding` 是设备私有数据
- 设备 1 和设备 2 对同一 `sync_item_id` 可拥有不同的 `local_path`

### 3.4 本地持久化模型拆分

改造 `PersistedStore`：

- 保留本地运行态
- 但不再将共享同步项定义仅保存在本地 `config.mappings`

建议拆分为：

- `LocalAppConfig`
  - `webdav`
  - `sync`
- `SharedConfigCache`
  - `space_id`
  - `sync_items`
  - `fetched_at`
- `DeviceConfig`
  - `device_id`
  - `device_name`
  - `bindings`
- `file_states`
- `sync_logs`

兼容期可保留旧字段 `mappings`，但应只作为迁移来源，不再作为主业务字段。

## 4. WebDAV 远端结构改造

### 4.1 目标远端目录结构

建议统一为：

- `remote_dir/<space_id>/.config/sync-items.json`
- `remote_dir/<space_id>/.devices/<device_id>/bindings.json`
- `remote_dir/<space_id>/.devices/<device_id>/status.json`
- `remote_dir/<space_id>/<remote_path>`

例如：

- `my-sync/default/.config/sync-items.json`
- `my-sync/default/.devices/<device_id>/bindings.json`
- `my-sync/default/git/.gitconfig`

### 4.2 WebDAV 路径方法重构

改造对象：

- [src-tauri/src/app/webdav.rs](/D:/Prjs/my-sync/src-tauri/src/app/webdav.rs)
- [src-tauri/src/app/storage.rs](/D:/Prjs/my-sync/src-tauri/src/app/storage.rs)

需替换的核心思路：

- 废弃 `join_remote_segments_with_client`
- 改为按“共享内容路径”“共享配置路径”“设备元数据路径”分别构造

建议新增方法：

- `shared_root_segments()`
- `shared_file_url(remote_path)`
- `shared_config_url()`
- `device_meta_url(device_id, file_name)`
- `list_shared_entries(path)`

禁止继续使用的规则：

- 主文件路径拼接 `device_id`
- 浏览远端文件时默认落在设备私有目录

### 4.3 远端根准备逻辑调整

改造对象：

- [src-tauri/src/app/storage.rs](/D:/Prjs/my-sync/src-tauri/src/app/storage.rs)
- [src-tauri/src/app/webdav.rs](/D:/Prjs/my-sync/src-tauri/src/app/webdav.rs)

当前 `can_prepare_remote_root()` 依赖 `client_id`。

改造后：

- 仅要求 `base_url`、`username`、`remote_dir`、`space_id`
- `device_id` 不应成为共享目录可访问的前置条件

## 5. 共享配置同步改造

### 5.1 新增共享配置读写能力

改造对象：

- [src-tauri/src/app/webdav.rs](/D:/Prjs/my-sync/src-tauri/src/app/webdav.rs)
- [src-tauri/src/app/commands.rs](/D:/Prjs/my-sync/src-tauri/src/app/commands.rs)

需要新增能力：

- 读取 `sync-items.json`
- 写入 `sync-items.json`
- 首次不存在时自动初始化为空配置

建议新增方法：

- `fetch_shared_config()`
- `save_shared_config(shared_config)`

建议新增命令：

- `refresh_shared_config`
- `save_shared_sync_items`

说明：

- 当前的 `save_app_config` 只保存本地配置，不足以满足需求
- 后续应明确区分“保存本地设置”和“保存共享同步项”

### 5.2 新设备首次接入流程

改造对象：

- [src-tauri/src/app/commands.rs](/D:/Prjs/my-sync/src-tauri/src/app/commands.rs)
- [src/stores/syncApp.ts](/D:/Prjs/my-sync/src/stores/syncApp.ts)

目标流程：

1. 用户在设备 2 填写 WebDAV 连接信息和同步空间
2. 本地保存 WebDAV 设置
3. 拉取 `sync-items.json`
4. 更新前端共享同步项列表
5. 针对每个同步项生成当前设备的本地路径候选
6. 能自动推断的直接创建 `DeviceBinding`
7. 不能推断的标记为 `pending_bind`

当前 `bootstrap()` 只会加载本地状态，后续必须增加“共享配置拉取”步骤。

## 6. 同步主流程改造

### 6.1 遍历单位从 `mapping` 改为“共享同步项 + 当前设备绑定”

改造对象：

- [src-tauri/src/app/sync.rs](/D:/Prjs/my-sync/src-tauri/src/app/sync.rs)

当前：

- `sync_all_mappings(config, file_states)`

改造后建议：

- `sync_all_items(local_config, shared_sync_items, device_bindings, file_states)`

每次同步时必须先为每个共享同步项查找当前设备的绑定关系。

分支规则：

- 有绑定且本地路径有效：执行正常同步
- 有绑定但本地路径不存在：根据策略决定恢复或标为异常
- 无绑定：标记 `pending_bind`，跳过文件同步

### 6.2 单项同步输入结构调整

当前 `sync_single_mapping()` 直接依赖 `FileMapping`。

建议改为：

- 输入 `SyncItem`
- 输入 `Option<DeviceBinding>`
- 输入当前 `FileRuntimeState`

这样才能表达：

- 共享同步项已存在
- 当前设备尚未绑定本地路径

### 6.3 运行态 key 规则调整

当前 `file_states` 以 `mapping.id` 为 key。

改造后建议继续使用共享同步项 ID：

- `file_states[sync_item_id]`

原因：

- 共享同步项在多设备之间是稳定对象
- 当前设备只是在这个对象上追加本地绑定

### 6.4 `pending_bind` 状态落地

改造对象：

- [src-tauri/src/app/models.rs](/D:/Prjs/my-sync/src-tauri/src/app/models.rs)
- [src/utils/appState.ts](/D:/Prjs/my-sync/src/utils/appState.ts)
- [src/components/views/SettingsView.vue](/D:/Prjs/my-sync/src/components/views/SettingsView.vue)

需要新增状态：

- `pending_bind`

语义：

- 同步项已存在于共享配置
- 当前设备尚未确定有效本地路径
- 这不是同步失败

当前 `evaluate_readiness()` 把本地文件不存在直接判定为不可同步，这一逻辑必须重写。

## 7. 路径推断改造

### 7.1 从共享同步项恢复本地候选路径

改造对象：

- [src/utils/appState.ts](/D:/Prjs/my-sync/src/utils/appState.ts)
- [src/stores/syncApp.ts](/D:/Prjs/my-sync/src/stores/syncApp.ts)
- 后端新增路径推断辅助逻辑

当前只有：

- `inferRemotePathFromLocal(localPath)`

后续必须新增反向能力：

- 根据 `path_template` 推断当前设备本地路径
- 根据同步项类型生成候选路径
- 基于当前系统用户目录生成 `C:\Users\admin\.gitconfig` 这类路径

建议顺序：

1. 优先使用 `path_template`
2. 命中预置规则
3. 基于用户目录兜底生成候选
4. 仍失败则进入 `pending_bind`

### 7.2 新增 `path_template`

改造对象：

- [src-tauri/src/app/models.rs](/D:/Prjs/my-sync/src-tauri/src/app/models.rs)
- [src/types/app.ts](/D:/Prjs/my-sync/src/types/app.ts)
- [src/components/views/SettingsView.vue](/D:/Prjs/my-sync/src/components/views/SettingsView.vue)

建议：

- 在共享同步项上新增 `path_template`
- 常见值如 `%USERPROFILE%\\.gitconfig`

作用：

- 设备 2 拉取共享配置后，可直接解析到自己的用户目录

### 7.3 现有新增映射流程调整

当前前端新增映射流程：

- 选择本地文件
- 生成 `mapping`
- 通过 `inferRemotePathFromLocal` 推导 `remotePath`
- 保存到本地 `config.mappings`

改造后：

1. 选择本地文件
2. 生成或填写共享 `SyncItem`
3. 自动推导 `remote_path`
4. 自动推导或填写 `path_template`
5. 保存共享配置到 WebDAV
6. 同时在当前设备保存 `DeviceBinding`

## 8. 前端状态与界面改造

### 8.1 类型系统改造

改造对象：

- [src/types/app.ts](/D:/Prjs/my-sync/src/types/app.ts)

需新增类型：

- `SyncItem`
- `DeviceBinding`
- `SharedConfig`
- `DeviceInfo`

需调整类型：

- `AppConfig` 不再直接内含 `mappings`
- `AppSnapshot` 需要同时返回：
  - 本地配置
  - 共享同步项
  - 当前设备绑定
  - 运行时状态

### 8.2 Store 改造

改造对象：

- [src/stores/syncApp.ts](/D:/Prjs/my-sync/src/stores/syncApp.ts)

当前 store 只维护：

- `config`
- `runtime`
- `logs`
- `remoteEntries`

改造后建议增加：

- `sharedSyncItems`
- `deviceBindings`
- `spaceId`
- `deviceInfo`

现有需要重写的逻辑：

- `remoteRootPath`
  - 从 `remoteDir/clientId` 改为 `remoteDir/spaceId`
- `canBrowseRemote`
  - 去掉对 `clientId` 的依赖
- `snapshotConfig`
  - 不再把所有映射都塞进本地配置
- `resetConfig`
  - 增加共享配置和绑定恢复
- `addMapping`
  - 改为“新增同步项”

### 8.3 设置页改造

改造对象：

- [src/components/views/SettingsView.vue](/D:/Prjs/my-sync/src/components/views/SettingsView.vue)

需要调整的展示：

- 将“客户端 ID”改为“设备 ID”
- 新增“同步空间”
- 远端根路径展示改为 `remoteDir/spaceId`
- 文件映射区域改为：
  - 共享同步项列表
  - 当前设备本地绑定状态

每项应至少展示：

- 同步项名称
- 远端逻辑路径
- 路径模板
- 当前设备本地路径
- 当前绑定状态

### 8.4 状态色与文案调整

改造对象：

- [src/utils/appState.ts](/D:/Prjs/my-sync/src/utils/appState.ts)

需要补充：

- `pending_bind` 的文案和色彩
- “待绑定”不应被归类为错误

## 9. 后端命令改造

### 9.1 `load_app_state` 返回结构扩展

改造对象：

- [src-tauri/src/app/commands.rs](/D:/Prjs/my-sync/src-tauri/src/app/commands.rs)

当前只返回：

- `config`
- `runtime`

改造后建议返回：

- `local_config`
- `shared_sync_items`
- `device_bindings`
- `device_info`
- `runtime`

### 9.2 `save_app_config` 拆分

当前 `save_app_config` 同时承担：

- 本地配置保存
- WebDAV 根目录准备

后续建议拆为：

- `save_local_settings`
- `save_shared_sync_items`
- `save_device_bindings`

原因：

- 三类数据的权限边界和落盘位置不同

### 9.3 新增同步空间初始化命令

建议新增命令：

- `initialize_sync_space`

职责：

- 确保 `remote_dir/<space_id>` 存在
- 确保 `.config/` 和 `.devices/<device_id>/` 存在
- 缺少共享配置时初始化空的 `sync-items.json`

### 9.4 远端浏览命令语义调整

当前：

- `list_remote_files(path)` 浏览的是当前 `remote_dir/client_id`

改造后：

- 默认浏览共享内容目录 `remote_dir/<space_id>/`
- 隐藏 `.config` 和 `.devices` 等系统目录，或以特殊方式区分显示

## 10. 设备 ID 改造

### 10.1 生成规则替换

改造对象：

- [src-tauri/src/app/storage.rs](/D:/Prjs/my-sync/src-tauri/src/app/storage.rs)

当前：

- `generate_client_id()` 使用时间戳

改造后：

- 废弃 `generate_client_id()`
- 新增 `generate_device_id()`
- 依据以下候选数据归一化后哈希：
  - 机器 UUID
  - 主板序列号
  - BIOS 序列号
  - CPU 标识的一部分
  - 主系统盘序列号

实现要求：

- 过滤空值、默认值和无效占位值
- 固定字段顺序拼接
- 使用稳定哈希算法输出

### 10.2 兼容期策略

建议：

- 若本地已有旧 `client_id`，先保留用于兼容读取
- 新版本启动后计算新 `device_id`
- 设备元数据优先写入新路径
- 迁移完成后逐步移除旧 `client_id` 展示和依赖

## 11. 迁移方案

### 11.1 本地配置迁移

迁移来源：

- 旧 `AppConfig.mappings`

迁移动作：

1. 将每条旧 `mapping` 转成 `SyncItem`
2. 提取：
   - `id`
   - `name`
   - `remote_path`
3. 生成当前设备的 `DeviceBinding`
   - `sync_item_id = mapping.id`
   - `local_path = mapping.local_path`

如果能识别常见路径，应尝试自动生成 `path_template`。

### 11.2 远端目录迁移

迁移来源：

- 旧结构 `remote_dir/client_id/...`

迁移目标：

- 新结构 `remote_dir/space_id/...`

迁移步骤建议：

1. 检测是否存在旧 `client_id` 子目录
2. 如果存在，提示当前结构为“按设备隔离”
3. 将当前设备目录下的共享内容迁移到 `space_id`
4. 写入 `sync-items.json`
5. 写入当前设备的 `bindings.json`
6. 保留迁移日志

### 11.3 冲突预警

如果多个旧设备目录中存在同名远端文件但内容不同，迁移时必须提示：

- 存在潜在内容冲突
- 不应静默覆盖
- 应要求用户选择保留策略或进入迁移冲突列表

## 12. 实施阶段建议

### 12.1 第一阶段

目标：

- 去掉共享内容路径中的 `client_id`
- 引入 `space_id`
- 引入共享同步项文件 `sync-items.json`
- 新设备可拉取共享同步项定义

第一阶段允许：

- 设备绑定仍主要保存在本地
- 先只实现基础路径模板

### 12.2 第二阶段

目标：

- 引入 `DeviceBinding` 远端备份
- 支持 `pending_bind`
- 支持自动路径推断
- 重构设置页为“共享同步项 + 设备绑定”

### 12.3 第三阶段

目标：

- 设备 ID 升级为硬件特征哈希
- 完整迁移旧版 `client_id`
- 完整支持新设备自动恢复绑定

## 13. 明确禁止的做法

后续改造中应明确避免以下方案：

- 继续将 `device_id` 拼到共享远端文件路径中
- 继续把 `local_path` 作为共享配置的一部分直接同步
- 让设备 2 直接继承设备 1 的绝对路径
- 将“待绑定”判定为同步失败
- 继续把本地 `mappings` 视为唯一真相来源

## 14. 最终验收标准

改造完成后，必须至少满足以下场景：

1. 设备 1 新建同步项 `Git 配置`
2. 本地绑定为 `C:\Users\LuoLong\.gitconfig`
3. 共享配置自动写入 WebDAV
4. 共享内容路径为 `remote_dir/default/git/.gitconfig`
5. 设备 2 仅填写 WebDAV 和同步空间
6. 设备 2 自动获取 `Git 配置` 这一同步项定义
7. 设备 2 自动推断候选路径 `C:\Users\admin\.gitconfig`，或进入“待绑定”
8. 设备 1 修改 `.gitconfig` 后上传共享远端
9. 设备 2 自动拉取并写入自己的本地路径

只有满足这组场景，当前产品才真正从“按客户端隔离的同步工具”变为“跨设备共享配置的同步工具”。
