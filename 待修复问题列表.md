- [x] 我拖拽一个大于1M的文件到设置页面，居然成功了
- [x] Tauri 最佳实践（使用 Command 模式、State 管理、耗时操作异步执行）
- [x] Rust 编码规范（统一 rustfmt、cargo clippy、snake_case 命名）
- [x] 代码行数限制 (Rust)：理想 300-500 行，超过 1000 行建议 `mod` 模块化重构
- [x] Vue 3 / TypeScript 最佳实践（优先 `<script setup>`、逻辑抽离至 Composables、Pinia 状态管理）
- [x] 代码行数限制 (Vue/TS)：SFC 总行数建议 < 500，`<script>` 块 < 300，单个函数 < 50，复杂业务强制抽离为 Composable

补充说明：
- 前端已通过 `vue-tsc --noEmit` 与 `eslint src --quiet` 校验。
- Rust 已通过 `cargo fmt --all --check`，并完成 `src-tauri/src/app/` 模块化拆分；当前环境运行 `cargo check` / `cargo clippy` 时持续触发 Windows 页文件/内存异常，属于机器环境限制，不是本次改动暴露出的语法错误。
